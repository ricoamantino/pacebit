# Desenvolvimento local: copie para `.env.local` e use o client ID real associado ao ID estável.
# Testes automatizados: `.env.test` contém somente um identificador sintático e não funcional.
# Distribuição: o GitHub Actions recebe o client ID real por uma variável pública do repositório.
WXT_GOOGLE_OAUTH_CLIENT_ID=
